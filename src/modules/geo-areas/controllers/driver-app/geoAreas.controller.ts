import { resources } from "@/utils/resources";
import model from "../../models/geoAreas.model";
export const { index, create, edit, update, deleteItem } = resources(model)

export const surgeGeoAreas = async () => {

}
